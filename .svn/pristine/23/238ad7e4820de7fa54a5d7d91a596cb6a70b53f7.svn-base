
package com.velotech.fanselection.design.dao;

import java.util.ArrayList;

import java.util.List;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.hibernate.Criteria;
import org.hibernate.criterion.DetachedCriteria;
import org.hibernate.criterion.Projections;
import org.hibernate.criterion.Restrictions;
import org.hibernate.criterion.Subqueries;
import org.springframework.stereotype.Repository;

//import com.velotech.fanselection.models.Tbl12ModelPerformance;
import com.velotech.fanselection.utils.HibernateSession;

@Repository
public class SelectionRangeDaoImpl extends HibernateSession implements SelectionRangeDao {

	static Logger log = LogManager.getLogger(SelectionRangeDaoImpl.class.getName());

	/*
	 * @Override public List<Tbl12ModelPerformance> getRecords(String series, String
	 * pumpModel, String performanceCurveNo) {
	 * 
	 * List<Tbl12ModelPerformance> ans = new ArrayList<>(); try { DetachedCriteria
	 * detachedCriteria = DetachedCriteria.forClass(Tbl12ModelPerformance.class);
	 * detachedCriteria.createAlias("tbl02Modelmaster", "tbl02Modelmaster");
	 * detachedCriteria.createAlias("tbl02Modelmaster.tbl01Pumptype",
	 * "tbl01Pumptype"); detachedCriteria.createAlias("tbl09Performancecurvemaster",
	 * "tbl09Performancecurvemaster");
	 * 
	 * detachedCriteria.setProjection(Projections.property("id"));
	 * 
	 * if (!series.equals("All")) {
	 * detachedCriteria.add(Restrictions.like("tbl01Pumptype.series", series)); } if
	 * (!pumpModel.equals("")) {
	 * detachedCriteria.add(Restrictions.like("tbl02Modelmaster.pumpModel",
	 * pumpModel)); } if (!performanceCurveNo.equals("")) {
	 * detachedCriteria.add(Restrictions.like(
	 * "tbl09Performancecurvemaster.performanceCurveNo", performanceCurveNo)); }
	 * 
	 * Criteria criteria =
	 * getSessionWFilter().createCriteria(Tbl12ModelPerformance.class);
	 * criteria.add(Subqueries.propertyIn("id", detachedCriteria)); ans =
	 * criteria.list(); } catch (Exception e) { log.error(e.getMessage(), e); }
	 * return ans; }
	 */

}
