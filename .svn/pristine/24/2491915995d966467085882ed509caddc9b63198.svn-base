
package com.velotech.fanselection.design.performance.dao;

import java.util.ArrayList;

import java.util.List;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.hibernate.Criteria;
import org.hibernate.FetchMode;
import org.hibernate.criterion.Restrictions;
import org.springframework.stereotype.Repository;

import com.velotech.fanselection.design.performance.dto.PerformanceDataTabularDto;
//import com.velotech.fanselection.models.Tbl12ModelPerformance;
import com.velotech.fanselection.utils.HibernateSession;

@Repository
public class PerformanceDataTableDaoImpl extends HibernateSession implements PerformanceDataTabularDao {

	static Logger log = LogManager.getLogger(PerformanceDataTableDaoImpl.class.getName());

	/*
	 * @Override public List<Tbl12ModelPerformance>
	 * getModelMaster(PerformanceDataTabularDto dto) {
	 * 
	 * List<Tbl12ModelPerformance> ans = new ArrayList<>(); try {
	 * 
	 * Criteria criteria =
	 * getSessionWFilter().createCriteria(Tbl12ModelPerformance.class).createAlias(
	 * "tbl02Modelmaster", "tbl02Modelmaster")
	 * .add(Restrictions.eq("tbl02Modelmaster.availability", true))
	 * .add(Restrictions.eq("tbl02Modelmaster.tbl01Pumptype.id", dto.getSeriesId()))
	 * .setFetchMode("tbl09Performancecurvemaster", FetchMode.EAGER);
	 * 
	 * ans = criteria.list(); for (Tbl12ModelPerformance tbl12ModelPerformance :
	 * ans) { tbl12ModelPerformance.getTbl09Performancecurvemaster().
	 * getTbl10PerformanceDias().iterator();
	 * 
	 * } } catch (Exception e) {
	 * 
	 * log.error(e.getMessage(), e); } return ans; }
	 */
}