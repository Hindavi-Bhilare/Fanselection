
package com.velotech.fanselection.design.performance.controller;

import java.io.IOException;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.velotech.fanselection.design.performance.dto.PerformanceDataTabularDto;
import com.velotech.fanselection.design.performance.service.PerformanceDataTabularService;
import com.velotech.fanselection.utils.ApplicationResponse;

@RestController
@RequestMapping(value = "design/performance/performancedatatabular")
public class PerformanceDataTabularController {

	static Logger log = LogManager.getLogger(PerformanceDataTabularController.class.getName());
	/*
	 * @Autowired private PerformanceDataTabularService service;
	 * 
	 * @RequestMapping(value = "/getcharts", method = RequestMethod.POST) public
	 * ApplicationResponse getCharts(PerformanceDataTabularDto dto) throws
	 * JsonParseException, JsonMappingException, IOException {
	 * 
	 * return service.getCharts(dto); }
	 * 
	 * @RequestMapping(value = "/downloadexcel", method = RequestMethod.POST) public
	 * ApplicationResponse downloadExcel(PerformanceDataTabularDto dto) throws
	 * JsonParseException, JsonMappingException, IOException {
	 * 
	 * return service.downloadExcel(dto); }
	 */

}
